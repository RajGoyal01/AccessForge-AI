import { notFound } from "next/navigation"; import { StoreShell } from "@/components/shell"; import { ProductVisual } from "@/components/product-visual"; import { getProduct } from "@/lib/products";
export default async function ProductPage({params}:{params:Promise<{productId:string}>}){const {productId}=await params;const product=getProduct(productId);if(!product)notFound();return <StoreShell><div className="detail"><ProductVisual product={product}/><div className="detail-copy"><p className="breadcrumbs">Products / {product.category}</p><h1>{product.name}</h1><p className="price">${product.price}.00</p><p>{product.description} Designed with durable materials and made for long-term use.</p><button className="primary-button">Add to cart</button><h3>Details</h3><p>Free delivery over $75 · 30-day returns · Two-year guarantee</p></div></div></StoreShell>}
