import Link from "next/link"; import { StoreShell } from "@/components/shell"; import { ProductVisual } from "@/components/product-visual"; import { products } from "@/lib/products";
export default function CartPage(){const product=products[0]!;return <StoreShell><header className="page-hero"><p className="eyebrow">Your selection</p><h1>Shopping cart</h1></header><div className="cart-grid"><article className="cart-item"><ProductVisual product={product} compact/><div><strong>{product.name}</strong><p>Quantity: 1</p></div><span>${product.price}.00</span></article><p><strong>Total: ${product.price}.00</strong></p><Link className="primary-link" href="/checkout">Continue to checkout</Link></div></StoreShell>}
