import{describe,expect,it}from"vitest";import{exactReplace,hashContent}from"./file-utils";describe("controlled replacement",()=>{it("replaces one exact occurrence",()=>expect(exactReplace("abc","b","B")).toBe("aBc"));it("rejects missing and ambiguous text",()=>{expect(()=>exactReplace("abc","x","X")).toThrow();expect(()=>exactReplace("aa","a","A")).toThrow()});it("hashes deterministically",()=>expect(hashContent("a")).toHaveLength(64))});
