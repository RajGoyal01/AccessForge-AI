import path from "node:path"; import { AppError } from "@/lib/errors";
const blocked=new Set([".env","package.json","package-lock.json","next.config.ts"]);
export function resolveNovaMartPath(relativePath:string){if(!relativePath||path.isAbsolute(relativePath)||relativePath.includes("\0"))throw new AppError("FORBIDDEN","Unsafe source path.",403);const root=path.resolve(process.cwd(),"../novamart");const resolved=path.resolve(root,relativePath);if(!(resolved===root||resolved.startsWith(root+path.sep)))throw new AppError("FORBIDDEN","Source path leaves the NovaMart workspace.",403);const parts=relativePath.split(/[\\/]/);if(parts.some(part=>["node_modules",".git",".next"].includes(part))||blocked.has(path.basename(relativePath))||path.basename(relativePath).startsWith(".env"))throw new AppError("FORBIDDEN","This file is not an allowed repair target.",403);return{root,absolutePath:resolved,relativePath:relativePath.replace(/\\/g,"/")}}
