import{notFound}from"next/navigation";import{db}from"@/lib/db/client";import{Status}from"@/components/status";import{RepairControls}from"@/components/repair-controls";
export const dynamic="force-dynamic";export default async function RepairPage({params}:{params:Promise<{repairId:string}>}){const repair=await db.repair.findUnique({where:{id:(await params).repairId},include:{project:true,issue:true,evaluations:{orderBy:{createdAt:"desc"},take:1}}});if(!repair)notFound();return <><header className="page-head"><div><p className="eyebrow">Human review centre</p><h1>{repair.issue.title}</h1><p className="mono">{repair.targetFile}</p></div><div><Status value={repair.status}/> <span className="demo-label">{repair.generatedBy}</span></div></header><div className="grid three-col"><article className="panel"><p className="eyebrow">Problem</p><h3>{repair.issueSummary??repair.issue.title}</h3><p className="muted">{repair.userImpact}</p></article><article className="panel"><p className="eyebrow">Approach</p><h3>{repair.proposedApproach}</h3><p className="muted">{repair.explanation}</p></article><article className="panel"><p className="eyebrow">Risk and confidence</p><h3>{repair.riskLevel} risk · {Math.round(repair.confidence*100)}%</h3><p className="muted">{repair.assumptions}</p></article></div><section className="panel" style={{marginTop:18}}><div className="page-head"><div><p className="eyebrow">Code review</p><h2>{repair.targetFile}</h2></div><span className="status waiting_for_approval">Exact replacement</span></div><div className="code-grid"><div><h3>Original</h3><pre className="code">{repair.originalCode}</pre></div><div><h3>Proposed</h3><pre className="code">{repair.proposedCode}</pre></div></div><details style={{marginTop:16}}><summary>View unified diff</summary><pre className="code diff" style={{minHeight:180}}>{repair.unifiedDiff}</pre></details><h3>Validation steps</h3><p className="muted" style={{whiteSpace:"pre-line"}}>{repair.validationSteps}</p></section><div style={{marginTop:18}}><RepairControls repairId={repair.id} status={repair.status} evaluationId={repair.evaluations[0]?.id}/></div>{repair.errorMessage?<details className="error" style={{marginTop:18}}><summary>Operation details</summary><p>{repair.errorMessage}</p></details>:null}</>}
