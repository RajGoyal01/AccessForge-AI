import{db}from"@/lib/db/client";import{Status}from"@/components/status";export const dynamic="force-dynamic";export default async function ActivityPage(){const events=await db.activityEvent.findMany({orderBy:{createdAt:"desc"},take:100,include:{project:true}});return <><header className="page-head"><div><p className="eyebrow">Audit trail</p><h1>Activity</h1></div></header><section className="panel activity">{events.length?events.map(event=><div className="event" key={event.id}><time>{event.createdAt.toLocaleString()}</time><span>{event.agent}</span><p><Status value={event.status}/><br/>{event.message}<br/><small className="muted">{event.project.name}</small></p></div>):<div className="empty">Activity events appear as real operations run.</div>}</section></>}
