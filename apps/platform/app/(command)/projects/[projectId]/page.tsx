import{notFound}from"next/navigation";import{projectService}from"@/lib/db/services/projects";import{db}from"@/lib/db/client";import{Status,Severity}from"@/components/status";import{StartScanButton}from"@/components/action-buttons";import Link from"next/link";
export const dynamic="force-dynamic";export default async function ProjectPage({params}:{params:Promise<{projectId:string}>}){const{id}= {id:(await params).projectId};const[project,stats]=await Promise.all([projectService.getById(id),projectService.stats(id)]);if(!project)notFound();const[issues,repairs,evaluations,activity]=await Promise.all([db.issue.findMany({where:{scan:{projectId:id}},orderBy:{createdAt:"desc"},take:8}),db.repair.findMany({where:{projectId:id},orderBy:{id:"desc"},take:8}),db.evaluation.findMany({where:{repair:{projectId:id}},orderBy:{createdAt:"desc"},take:8}),db.activityEvent.findMany({where:{projectId:id},orderBy:{createdAt:"desc"},take:8})]);return <><header className="page-head"><div><p className="eyebrow">{project.projectType==="BUNDLED_DEMO"?"Full repair capability":"Audit only — external source modification unavailable"}</p><h1>{project.name}</h1><p className="mono">{project.targetUrl}</p></div><StartScanButton projectId={id}/></header><div className="grid metrics"><article className="panel metric"><span>Latest score</span><strong>{stats?.latestScore??"—"}</strong></article><article className="panel metric"><span>Scans</span><strong>{stats?.scanCount??0}</strong></article><article className="panel metric"><span>Repairs</span><strong>{stats?.repairCount??0}</strong></article><article className="panel metric"><span>Capability</span><strong style={{fontSize:17}}>{project.projectType==="BUNDLED_DEMO"?"Repair + rollback":"Audit only"}</strong></article></div><div className="grid two-col" style={{marginTop:18}}><section className="panel"><h2>Recent scans</h2>{project.scans.length?<table className="table"><tbody>{project.scans.map(scan=><tr key={scan.id}><td><Link href={`/scans/${scan.id}`}>{scan.id.slice(-8)}</Link></td><td><Status value={scan.status}/></td><td>{scan.finalScore??"—"}</td></tr>)}</tbody></table>:<div className="empty">No scans yet. Start a real browser audit.</div>}<h2>Issues</h2>{issues.map(issue=><Link className="issue-card" href={`/issues/${issue.id}`} key={issue.id}><Severity value={issue.impact}/><strong>{issue.title}</strong></Link>)}</section><aside className="panel"><h2>Repair and evaluation</h2>{repairs.length?repairs.map(repair=><Link className="issue-card" href={`/repairs/${repair.id}`} key={repair.id}><Status value={repair.status}/><strong>{repair.targetFile}</strong></Link>):<div className="empty">Repair proposals appear after source mapping.</div>}<h2>Activity</h2><div className="activity">{activity.map(event=><div className="event" key={event.id}><time>{event.createdAt.toLocaleTimeString()}</time><span>{event.agent}</span><p>{event.message}</p></div>)}</div>{evaluations.length?<Link href={`/evaluations/${evaluations[0]!.id}`}>Open latest evaluation →</Link>:null}</aside></div></>}
