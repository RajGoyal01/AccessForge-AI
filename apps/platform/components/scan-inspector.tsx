"use client";import{useState}from"react";import Link from"next/link";import type{Issue}from"@prisma/client";import{Severity}from"./status";
export function ScanInspector({screenshot,issues}:{screenshot:string|null;issues:Issue[]}){const[filter,setFilter]=useState("ALL");const[markers,setMarkers]=useState(true);const shown=issues.filter(issue=>filter==="ALL"||issue.impact===filter);return <div className="grid two-col"><section className="panel"><div className="page-head" style={{marginBottom:16}}><div><p className="eyebrow">Visual inspector</p><h2>Captured website</h2></div><div><select className="input" style={{width:150}} aria-label="Severity filter" value={filter} onChange={e=>setFilter(e.target.value)}><option>ALL</option><option>CRITICAL</option><option>SERIOUS</option><option>MODERATE</option><option>MINOR</option></select><button className="button-secondary" style={{marginLeft:8}} onClick={()=>setMarkers(!markers)}>{markers?"Hide":"Show"} markers</button></div></div>{screenshot?<div className="screenshot">{/* eslint-disable-next-line @next/next/no-img-element */}<img src={`/api/screenshots/${screenshot.split("/").at(-1)}`} alt="Full-page screenshot captured during the accessibility scan"/>{markers?shown.map((issue,index)=>issue.boundingX!==null&&issue.boundingY!==null?<Link className="marker" aria-label={`Issue ${index+1}: ${issue.title}`} key={issue.id} href={`/issues/${issue.id}`} style={{left:`${((issue.boundingX??0)/1440)*100}%`,top:`${issue.boundingY??0}px`}}>{index+1}</Link>:null):null}</div>:<div className="empty">Screenshot unavailable for this scan.</div>}</section><aside className="panel"><p className="eyebrow">Detected issues</p><h2>{shown.length} affected elements</h2><div className="issue-list">{shown.length?shown.map(issue=><Link className="issue-card" href={`/issues/${issue.id}`} key={issue.id}><Severity value={issue.impact}/><strong>{issue.title}</strong><span className="mono">{issue.selector}</span>{issue.repairAvailable?<p className="muted">Repair recipe available</p>:null}</Link>):<div className="empty">No issues match this filter.</div>}</div></aside></div>}
